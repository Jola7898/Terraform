# Dropped in favor of the VGGT batch pipeline (rtvio/src/rtvio/vggt_reconstruct.py) -
# see rtviomap/session.md for why it existed. Kept on disk, not in git: still a
# working WSL2 setup + adapter scaffold if the LingBot-Map direction is revisited,
# but not the chosen path going forward.
/rtviomap/

# Screenshots and presentation exports dropped at the repo root - not source.
/image*.png
/ppt_flowcharts/
/paper/
# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
TEAM- TERRAFORM SIH2026.pdf
paper_overleaf.zip
paper_overleaf.pdf
/slides/
